def if_possible(size, pages):
    if size <= len(pages):
        print("impossible")

def enough_pages(pages):
    for k in (len(pages)+1):
        pages[k] = int
        if pages[k] != int(k) * 2:
            print("impossible")

def tape(pages):
    areaA1 = 2**((-5)/4)
    for n in 1:n:
        areaA[n] = areaA[n-1]/2
        


            



def main():
    size = input()
    size = int
    pages = input()
    pages = list
    if_possible(size, pages)
    enough_pages(pages)
    tape()
    
    pass

if __name__ == "main":
    main()